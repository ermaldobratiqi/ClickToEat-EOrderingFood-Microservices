@*@model LoggedOutViewModel

@{
    // set this so the layout rendering sees an anonymous user
    ViewData["signed-out"] = true;
}

<style>
    .LogoutText {
        color: white;
    }

    .logoutImg {
        display: block;
        margin-left: auto;
        margin-right: auto;
        width: 50%;
    }
</style>

<body>
    <div class="logged-out-page">
        <div class="logoutImg">
            <br />
            <img class="center" width="700" height="350" src="/images/see-you-again.jpg" />
        </div>
        <div class="LogoutText">
            <h1>
                Logout
                <br />
                <small>You are now logged out</small>
            </h1>
        </div>

        @if (Model.PostLogoutRedirectUri != null)
        {
            <div>
                Click <a class="PostLogoutRedirectUri" href="@Model.PostLogoutRedirectUri">here</a> to return to the
                <span>@Model.ClientName</span> application.
            </div>
        }

        @if (Model.SignOutIframeUrl != null)
        {
            <iframe width="0" height="0" class="signout" src="@Model.SignOutIframeUrl"></iframe>
        }
    </div>

</body>

@section scripts
{
    @if (Model.AutomaticRedirectAfterSignOut)
    {
        <script src="~/js/signout-redirect.js"></script>
    }
}*@


@model LoggedOutViewModel

@{
    // set this so the layout rendering sees an anonymous user
    ViewData["signed-out"] = true;
}

<style>
    .logged-out-page {
        background-color: #F6F6F6;
        display: flex;
        flex-direction: column;
        align-items: center;
        height: 100vh;
    }

    .logout-text {
        color: #333;
        margin-top: 5%;
    }

        .logout-text h1 {
            font-size: 3em;
            font-weight: 700;
            margin-bottom: 10px;
        }

        .logout-text small {
            font-size: 1.2em;
            font-weight: 400;
            display: block;
        }

    .logout-img {
        width: 70%;
        margin-top: 10%;
    }

    .post-logout-redirect {
        font-size: 1.2em;
        margin-top: 5%;
        color: #337ab7;
        cursor: pointer;
    }

    .client-name {
        font-weight: 700;
    }
</style>

<body>
    <div class="logged-out-page">
        <img class="logout-img" src="/images/see-you-again.jpg" />
        <div class="logout-text">
            <h1>
                Logout
                <br />
                <small>You are now logged out</small>
            </h1>
        </div>

        @if (Model.PostLogoutRedirectUri != null)
        {
            <div class="post-logout-redirect">
                Click <a href="@Model.PostLogoutRedirectUri">here</a> to return to the
                <span class="client-name">@Model.ClientName</span> application.
            </div>
        }

        @if (Model.SignOutIframeUrl != null)
        {
            <iframe width="0" height="0" class="signout" src="@Model.SignOutIframeUrl"></iframe>
        }
    </div>
</body>

@section scripts
    {
    @if (Model.AutomaticRedirectAfterSignOut)
    {
        <script src="~/js/signout-redirect.js"></script>
    }
}
